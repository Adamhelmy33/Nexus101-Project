Save the Nexus 101 logo files here with these EXACT filenames:

  logo-blue.png     ← Blue logo on transparent/white (used on light backgrounds)
  logo-white.png    ← White logo on transparent/dark (used on dark backgrounds — hero, footer)

Both PNG and SVG formats are supported — just keep the filenames as above (or change the path in src/components/Logo.jsx if you prefer SVG).
